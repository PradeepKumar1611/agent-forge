# Agent Factory Configuration
# Copy this file to .env and edit the values

# Default admin password (used only on first startup to create the admin user)
ADMIN_PASSWORD=admin123

# Flask session secret key (change this to any random string in production)
FLASK_SECRET_KEY=change-me-to-a-random-string
